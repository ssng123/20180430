<h1>연극/뮤지컬/영화 후기</h1>
<p>관람한 작품들의 후기를 남겨 주세요 :)</p>


<a href="/home/new">기록하기</a>
<br>
<br>
<% @posts.each do |x| %>  <!-- 한 row를 x라고 해보자 -->
    <%= x.id%> 번째 작품 : <br>
    제목 : <%= x.title%> <br>
    내용 : <%= x.content%> <br>
    <a href="home/show/<%=x.id%>"> 자세히보기 </a> <br>
    <a href="home/edit/<%=x.id%>"> 수정하기 </a> <br>
    <a href="home/destroy/<%=x.id%>"> 사라져 </a> <br>
    <!-- <%=x.id%>라고 되어있는 건 모든 글마다 고유의 링크가 필요하기 때문이다. 
    id를 통해 각각의 글의 고유 링크를 전송해 줄 수 있게 된다. -->
    <!-- /home/show/3인 경우, get 'home/show/:post_id' => 'home#show' 꼴로 자동으로 param 받아버린 거.
    params ={post_id => ~ }의 꼴로 대기타고 있음. 얘가 매칭이 되어서 3번으로 parameter가 추가 생성됨.
    show라는 액션은 특정 글의 정보를 확인할 수 있는 페이지로 넘어가게 해 주는 것. (view) -->
    ㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡ <br>
    <%end%>

<!-- each는 반복문 같은 거. 각각의 x에 대해서 ~~를 수행해라.
do ~ end 는 함수임을 알려준다. 그 안에 있는 각각의 개체에 대해서 동일한 작업을 반복해라. -->

<!-- 이렇게 하면 전체 글 목록을 불러올 수 있어요. 
그런데 index 페이지에는 제목만 있고 개별 글로 들어가는 링크가 필요한데..!  : goto 10 -->




